<%= form_for(task) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_field :title, placeholder: "New task..." %>
  </div>
  <%= f.hidden_field(:parent_id) unless task.parent_id.nil? %>
  <%= f.submit "Create", class: "btn btn-large btn-primary" %>
<% end %>
